--- a/dlls/shell32/shelllink.c
+++ b/dlls/shell32/shelllink.c
@@ -301,26 +301,1 @@
-    STARTUPINFOW si;
-    PROCESS_INFORMATION pi;
-    BOOL ret;
-    WCHAR app[MAX_PATH];
-    void *redir;
-    static const WCHAR winemenubuilderW[] = L"\\winemenubuilder.exe";
-    WINE_STRING_BUFFER_FIXED( buffer, MAX_PATH + sizeof(winemenubuilderW)/sizeof(WCHAR) + 2 );
-
-    GetSystemDirectoryW( app, MAX_PATH );
-    lstrcatW( app, winemenubuilderW );
-
-    if (FAILED(wine_buffer_strcpy( &buffer, app ))) return FALSE;
-    if (FAILED(wine_buffer_strcat( &buffer, args ))) return FALSE;
-
-    TRACE("starting %s\n", debugstr_w(wine_buffer_get(&buffer)));
-
-    memset(&si, 0, sizeof(si));
-    si.cb = sizeof(si);
-
-    Wow64DisableWow64FsRedirection( &redir );
-    ret = CreateProcessW( app, wine_buffer_get_write_ptr(&buffer), NULL, NULL, FALSE, DETACHED_PROCESS, NULL, NULL, &si, &pi );
-    Wow64RevertWow64FsRedirection( redir );
-
-    if (ret)
-    {
-        CloseHandle( pi.hProcess );
-        CloseHandle( pi.hThread );
-    }
-    wine_buffer_free( &buffer );
-    return ret;
+    return TRUE;
